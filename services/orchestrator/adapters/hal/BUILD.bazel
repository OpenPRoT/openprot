# Licensed under the Apache-2.0 license
# SPDX-License-Identifier: Apache-2.0

load("@rules_rust//rust:defs.bzl", "rust_library", "rust_test")

rust_library(
    name = "orchestrator_hal_adapters",
    srcs = [
        "src/gpio_boot_monitor.rs",
        "src/gpio_ready_monitor.rs",
        "src/gpio_reset_control.rs",
        "src/hal_boot_control.rs",
        "src/lib.rs",
    ],
    edition = "2024",
    visibility = ["//visibility:public"],
    deps = [
        "//hal/blocking",
        "//services/orchestrator/capabilities:orchestrator_capabilities",
        "@rust_crates//:embedded-hal",
    ],
)

# Host tests: build on the host platform, no kernel/QEMU.
rust_test(
    name = "orchestrator_hal_adapters_test",
    crate = ":orchestrator_hal_adapters",
)
